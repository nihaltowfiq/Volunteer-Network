import React from 'react';
import Events from '../Events/Events';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
            <Header></Header>
            <Events></Events>
            <h1>This is Home</h1>
        </div>
    );
};

export default Home;